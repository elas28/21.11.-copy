export default function FunctionalComponent() {
    
}
    // JSX Syntax Goes In
    return (
      <div>
        <h2>React aplikacija</h2>
        <p>Pozdrav, moje ime je {props.string.firstName} i imam {props.number.age} </p>
        <p>Radim kao {props.string.job}</p>
      </div>
    );
  
  